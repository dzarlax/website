---
title: "Home"
description: "Alexey Panfilov — Product, AI, and Systems. Senior PM and tinkerer based in Belgrade."
# Hugo's home placeholder is dropped in CI (.github/workflows/deploy.yml) —
# the static lander owns /. Exclude it from blog-sitemap.xml so the URL
# doesn't appear twice (also in the lander's /sitemap.xml).
sitemap:
  disable: true
---
