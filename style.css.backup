:root {
    /* ========================================
       BRAND COLOR SYSTEM (HSL-based)
       Change --brand-hue to shift entire color palette
       ======================================== */
    --brand-hue: 217;          /* Blue */
    --brand-sat: 91%;          /* Saturation */
    --brand-light: 60%;        /* Base lightness */
    /* Accent colors (automatically derived from brand) */
    --accent-primary: hsl(var(--brand-hue), var(--brand-sat), var(--brand-light));
    --accent-hover: hsl(var(--brand-hue), var(--brand-sat), 50%);
    --accent-active: hsl(var(--brand-hue), var(--brand-sat), 40%);
    --accent-glow: hsla(var(--brand-hue), var(--brand-sat), var(--brand-light), 0.3);
    /* Secondary accent (purple/indigo) */
    --accent-secondary: #6366F1;
    --accent-secondary-hover: #8B5CF6;
    /* Semantic colors */
    --accent-success: #10B981;
    --accent-success-hover: #34D399;
    /* ========================================
       NEUTRAL COLOR SYSTEM
       ======================================== */
    --bg-hue: 240;             /* Neutral base hue */
    /* Light mode backgrounds */
    --bg-primary: hsl(var(--bg-hue), 10%, 98%);
    --bg-secondary: #FFFFFF;
    --bg-tertiary: hsl(var(--bg-hue), 10%, 97%);
    --bg-elevated: #FFFFFF;
    /* Footer backgrounds (brand-based gradient) */
    --color-footer-bg: hsl(var(--brand-hue), var(--brand-sat), 25%);
    --color-footer-bg-alt: hsl(var(--brand-hue), var(--brand-sat), 15%);
    /* Footer text colors (high contrast on dark bg) */
    --text-footer-primary: hsl(0, 0%, 100%);
    --text-footer-secondary: hsla(0, 0%, 100%, 0.8);
    --text-footer-muted: hsla(0, 0%, 100%, 0.6);
    /* Light mode text */
    --text-primary: hsl(var(--bg-hue), 10%, 18%);
    --text-secondary: hsl(var(--bg-hue), 5%, 35%);
    --text-tertiary: hsl(var(--bg-hue), 5%, 48%);
    --text-inverted: #FFFFFF;
    --text-dark: hsl(var(--bg-hue), 10%, 4%);
    /* Light mode borders */
    --border-primary: hsl(var(--bg-hue), 10%, 91%);
    --border-secondary: hsl(var(--bg-hue), 5%, 82%);
    --border-focus: var(--accent-primary);
    /* Gradients */
    --gradient-primary: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
    --gradient-secondary: linear-gradient(135deg, var(--accent-secondary), var(--accent-secondary-hover));
    --gradient-success: linear-gradient(135deg, var(--accent-success), var(--accent-success-hover));
    /* Shadows */
    --shadow-sm: 0 2px 8px hsla(0, 0%, 0%, 0.04), 0 8px 16px hsla(0, 0%, 0%, 0.04);
    --shadow-md: 0 8px 16px hsla(0, 0%, 0%, 0.08), 0 16px 32px hsla(0, 0%, 0%, 0.06);
    --shadow-lg: 0 12px 32px var(--accent-glow);
    /* Spacing */
    --space-unit: 4px;
    --s-1: calc(var(--space-unit) * 1);   /* 4px */
    --s-2: calc(var(--space-unit) * 2);   /* 8px */
    --s-3: calc(var(--space-unit) * 3);   /* 12px */
    --s-4: calc(var(--space-unit) * 4);   /* 16px */
    --s-5: calc(var(--space-unit) * 5);   /* 20px */
    --s-6: calc(var(--space-unit) * 6);   /* 24px */
    --s-8: calc(var(--space-unit) * 8);   /* 32px */
    --s-10: calc(var(--space-unit) * 10); /* 40px */
    --s-12: calc(var(--space-unit) * 12); /* 48px */
    --section-padding: clamp(var(--s-10), 10vh, calc(var(--s-10) * 3));
    /* Border radius */
    --border-radius-md: 16px;
    /* ========================================
       RESPONSIVE LAYOUT SYSTEM
       ======================================== */
    --container-max-width: 1200px;
    --grid-columns: 1;
    --grid-gap: var(--s-6);
    /* Transitions */
    --transition-normal: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    /* Stagger animations */
    --stagger-delay: 0.1s;
    /* ========================================
       TYPOGRAPHY SCALE
       ======================================== */
    --f-base: 1rem;
    --f-sm: calc(var(--f-base) * 0.875);    /* 14px */
    --f-md: var(--f-base);                   /* 16px */
    --f-lg: calc(var(--f-base) * 1.125);    /* 18px */
    --f-xl: calc(var(--f-base) * 1.25);     /* 20px */
    --f-2xl: calc(var(--f-base) * 1.5);     /* 24px */
    --f-3xl: calc(var(--f-base) * 1.875);   /* 30px */
    --f-4xl: calc(var(--f-base) * 2.25);    /* 36px */
    --f-5xl: calc(var(--f-base) * 3);       /* 48px */
}
[dark-mode] {
    /* Dark mode backgrounds (inverted lightness) */
    --bg-primary: hsl(var(--bg-hue), 15%, 6%);
    --bg-secondary: hsl(var(--bg-hue), 15%, 10%);
    --bg-tertiary: hsl(var(--bg-hue), 15%, 15%);
    --bg-elevated: hsl(var(--bg-hue), 15%, 10%);
    /* Footer backgrounds (subtler gradient for dark mode) */
    --color-footer-bg: hsl(var(--brand-hue), var(--brand-sat), 12%);
    --color-footer-bg-alt: hsl(var(--bg-hue), 15%, 8%);
    /* Footer text colors (high contrast on dark bg) */
    --text-footer-primary: hsl(0, 0%, 98%);
    --text-footer-secondary: hsla(0, 0%, 98%, 0.75);
    --text-footer-muted: hsla(0, 0%, 98%, 0.5);
    /* Dark mode text */
    --text-primary: hsl(var(--bg-hue), 10%, 69%);
    --text-secondary: hsl(var(--bg-hue), 5%, 48%);
    --text-tertiary: hsl(var(--bg-hue), 5%, 35%);
    --text-inverted: #FFFFFF;
    --text-dark: hsl(var(--bg-hue), 10%, 94%);
    /* Dark mode borders */
    --border-primary: hsl(var(--bg-hue), 15%, 20%);
    --border-secondary: hsl(var(--bg-hue), 15%, 30%);
    /* Dark mode accent (lighter for contrast) */
    --accent-primary: hsl(var(--brand-hue), var(--brand-sat), 70%);
    --accent-hover: hsl(var(--brand-hue), var(--brand-sat), 80%);
    --accent-active: hsl(var(--brand-hue), var(--brand-sat), 60%);
    --accent-glow: hsla(var(--brand-hue), var(--brand-sat), 70%, 0.4);
    /* Dark mode shadows */
    --shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.4), 0 2px 6px rgba(0, 0, 0, 0.3);
    --shadow-md: 0 12px 24px rgba(0, 0, 0, 0.5), 0 6px 12px rgba(0, 0, 0, 0.4);
    --shadow-lg: 0 12px 32px hsla(var(--brand-hue), var(--brand-sat), 70%, 0.4);
}
/* ========================================
   RESPONSIVE GRID SYSTEM (Mobile First)
   ======================================== */
@media (min-width: 768px) {
    :root {
        --grid-columns: 2;
    }
    /* Reveal animations on desktop */
    .reveal,
    section {
        opacity: 0;
        transform: translateY(30px);
        transition: all 0.8s ease;
    }
    .reveal.active,
    section.visible {
        opacity: 1;
        transform: translateY(0);
    }
}
@media (min-width: 1024px) {
    :root {
        --grid-columns: 3;
    }
}
html {
    overflow-x: hidden;
}
body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    line-height: 1.7;
    letter-spacing: -0.01em;
    font-size: var(--f-md);
    color: var(--text-primary);
    background-color: var(--bg-primary);
    margin: 0;
    padding: 0;
    position: relative;
    transition: background-color var(--transition-normal), color var(--transition-normal);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
}
* {
    box-sizing: border-box;
}
::-webkit-scrollbar {
    width: 10px;
}
::-webkit-scrollbar-track {
    background: transparent;
}
::-webkit-scrollbar-thumb {
    background: var(--accent-primary);
    border-radius: 5px;
}
::-webkit-scrollbar-thumb:hover {
    background: var(--accent-hover);
}
.scroll-progress {
    position: fixed;
    top: 0;
    left: 0;
    width: 0%;
    height: 3px;
    background: var(--gradient-primary);
    z-index: 10001;
    transition: width 0.1s ease-out;
    box-shadow: 0 0 10px var(--accent-primary-glow);
    pointer-events: none;
}
[dark-mode] .scroll-progress {
    box-shadow: 0 0 10px var(--accent-primary-glow),
                0 0 20px rgba(var(--accent-color-rgb), 0.3);
}
.theme-toggle {
    position: relative;
    width: 44px;
    height: 24px;
    background: linear-gradient(135deg, var(--border-primary), var(--border-secondary));
    border-radius: var(--s-3);
    border: none;
    cursor: pointer;
    padding: var(--s-1);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    margin-left: var(--s-1);
    &::after {
        content: '';
        position: absolute;
        top: var(--s-1);
        left: var(--s-1);
        width: 18px;
        height: 18px;
        background: white;
        border-radius: 50%;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: 0 var(--s-1) var(--s-2) rgba(0, 0, 0, 0.2);
        z-index: 1;
    }
    [dark-mode] &::after {
        left: calc(100% - 21px);
        background: var(--accent-primary);
    }
    &:hover {
        transform: scale(1.05);
        box-shadow: 0 var(--s-2) var(--s-3) var(--accent-glow);
    }
}
.hamburger {
    display: none;
    flex-direction: column;
    gap: var(--s-1);
    background: transparent;
    border: none;
    cursor: pointer;
    padding: var(--s-2);
    z-index: 1001;
    pointer-events: auto !important;
    span {
        display: block;
        width: var(--s-5);
        height: var(--s-1);
        background: var(--text-primary);
        transition: all var(--transition-normal);
        border-radius: var(--s-1);
    }
    &.active span:nth-child(1) {
        transform: rotate(45deg) translate(var(--s-2), var(--s-2));
    }
    &.active span:nth-child(2) {
        opacity: 0;
    }
    &.active span:nth-child(3) {
        transform: rotate(-45deg) translate(calc(var(--s-2) * 1.5), calc(var(--s-2) * -1.5));
    }
}
.menu-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    z-index: 998;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.3s ease, visibility 0.3s ease;
}
.menu-overlay.active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
}
main {
    width: 100%;
}
section {
    padding-block: var(--section-padding);
    padding-inline: var(--s-6);
    position: relative;
    width: 100%;
}
.container {
    max-width: 1400px;
    margin: 0 auto;
}
/* ========================================
   INTRO SECTION (Nested & Logical)
   Hero section with animated background
   ======================================== */
.intro {
    min-block-size: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 160px var(--s-6) 120px;
    position: relative;
    overflow: hidden;
    background: var(--bg-primary);
    &::before {
        content: '';
        position: absolute;
        inset-block-start: -50%;
        inset-inline-start: -50%;
        inline-size: 200%;
        block-size: 200%;
        background: radial-gradient(circle at 50% 50%,
                rgba(37, 99, 235, 0.15),
                rgba(79, 70, 229, 0.15),
                rgba(255, 255, 255, 0) 50%);
        filter: blur(60px);
        animation: meshRotate 20s linear infinite;
        z-index: 0;
        opacity: 0.8;
    }
    [dark-mode] &::before {
        background: radial-gradient(circle at 50% 50%,
                hsla(var(--brand-hue), var(--brand-sat), 60%, 0.15),
                rgba(99, 102, 241, 0.15),
                rgba(15, 17, 21, 0) 50%);
    }
    &::after {
        content: '';
        position: absolute;
        inline-size: 400px;
        block-size: 400px;
        background: radial-gradient(circle, rgba(139, 92, 246, 0.1), transparent 70%);
        border-radius: 50%;
        animation: float 15s ease-in-out infinite;
        z-index: 0;
    }
    /* Animated background shapes */
    & .shape-1 {
        position: absolute;
        inline-size: 300px;
        block-size: 300px;
        background: radial-gradient(circle, hsla(var(--brand-hue), var(--brand-sat), 60%, 0.08), transparent 70%);
        border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%;
        animation: morph 20s ease-in-out infinite, float 12s ease-in-out infinite;
        z-index: 0;
    }
    & .shape-2 {
        position: absolute;
        inline-size: 250px;
        block-size: 250px;
        background: radial-gradient(circle, rgba(16, 185, 129, 0.08), transparent 70%);
        border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
        animation: morph 18s ease-in-out infinite reverse, float 14s ease-in-out infinite reverse;
        z-index: 0;
    }
    /* Profile container */
    & .profile-container {
        position: relative;
        margin-block-end: var(--s-10);
        z-index: 2;
    }
    & .profile-image {
        inline-size: 150px;
        block-size: 150px;
        border-radius: 50%;
        object-fit: cover;
        border: var(--s-1) solid white;
        box-shadow: 0 var(--s-2) var(--s-4) rgba(0, 0, 0, 0.1);
        animation: fadeIn 1s ease-out;
    }
    & .profile-badge {
        position: absolute;
        inset-block-end: 0;
        inset-inline-end: 0;
        background: var(--gradient-primary);
        color: white;
        border-radius: 50%;
        inline-size: 45px;
        block-size: 45px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: var(--f-xl);
        box-shadow: 0 var(--s-2) var(--s-4) rgba(0, 0, 0, 0.1);
        animation: fadeIn 1.5s ease-out;
    }
    /* Typography */
    & h2 {
        font-size: clamp(1.5rem, 3vw, 2.2rem);
        color: var(--accent-primary);
        margin-block-end: var(--s-8);
        font-weight: 500;
        position: relative;
        z-index: 1;
        text-align: center;
    }
    & p {
        font-size: clamp(1rem, 2vw, 1.25rem);
        max-inline-size: 700px;
        margin: 0 auto var(--s-10);
        color: var(--text-secondary);
        position: relative;
        z-index: 1;
        strong {
            color: var(--accent-primary);
            font-weight: 700;
        }
    }
}
/* Animations */
@keyframes meshRotate {
    0% {
        transform: rotate(0deg) translate(0, 0);
    }
    50% {
        transform: rotate(180deg) translate(10%, 10%);
    }
    100% {
        transform: rotate(360deg) translate(0, 0);
    }
}
@keyframes float {
    0%, 100% {
        transform: translateY(0) translateX(0) rotate(0deg);
    }
    25% {
        transform: translateY(-30px) translateX(20px) rotate(5deg);
    }
    50% {
        transform: translateY(-15px) translateX(-20px) rotate(-5deg);
    }
    75% {
        transform: translateY(-25px) translateX(10px) rotate(3deg);
    }
}
@keyframes morph {
    0%, 100% {
        border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%;
    }
    25% {
        border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
    }
    50% {
        border-radius: 30% 70% 50% 50% / 30% 30% 70% 70%;
    }
    75% {
        border-radius: 50% 50% 40% 60% / 50% 60% 40% 50%;
    }
}
.profile-container {
    position: relative;
    margin-bottom: 40px;
    z-index: 2;
}
.profile-image {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid white;
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    animation: fadeIn 1s ease-out;
}
.profile-badge {
    position: absolute;
    bottom: 0;
    right: 0;
    background: var(--gradient-primary);
    color: white;
    border-radius: 50%;
    width: 45px;
    height: 45px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--f-lg);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    animation: fadeIn 1.5s ease-out;
}
@keyframes float {
    0%, 100% {
        transform: translateY(0) translateX(0) rotate(0deg);
    }
    25% {
        transform: translateY(-30px) translateX(20px) rotate(5deg);
    }
    50% {
        transform: translateY(-15px) translateX(-20px) rotate(-5deg);
    }
    75% {
        transform: translateY(-25px) translateX(10px) rotate(3deg);
    }
}
@keyframes morph {
    0%, 100% {
        border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%;
    }
    25% {
        border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
    }
    50% {
        border-radius: 30% 70% 50% 50% / 30% 30% 70% 70%;
    }
    75% {
        border-radius: 50% 50% 40% 60% / 50% 60% 40% 50%;
    }
}
.typing-effect {
    border-right: none !important;
    white-space: nowrap;
    overflow: hidden;
    display: inline;
    max-width: fit-content;
    line-height: 1;
}
@keyframes typing {
    from {
        width: 0
    }
    to {
        width: 100%
    }
}
@keyframes blink-caret {
    from,
    to {
        border-color: transparent
    }
    50% {
        border-color: var(--accent-primary)
    }
}
/* ========================================
   SKILLS SECTION (Nested & Logical)
   ======================================== */
.skills {
    position: relative;
    overflow: hidden;
    &::before {
        content: '';
        position: absolute;
        inset-block-start: 0;
        inset-inline-end: 0;
        inline-size: 300px;
        block-size: 300px;
        background: var(--gradient-primary);
        opacity: 0.05;
        border-radius: 50%;
        translate: 150px -150px;
        z-index: -1;
    }
    &::after {
        content: '';
        position: absolute;
        inset-block-end: 0;
        inset-inline-start: 0;
        inline-size: 200px;
        block-size: 200px;
        background: var(--gradient-secondary);
        opacity: 0.05;
        border-radius: 50%;
        translate: -100px 100px;
        z-index: -1;
    }
}
.skills__container {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--s-6);
    margin: var(--s-10) auto 0;
    max-inline-size: var(--container-max-width);
}
.skills__section-title {
    grid-column: 1 / -1;
    inline-size: 100%;
    font-size: var(--f-xl);
    font-weight: 700;
    color: var(--text-dark);
    margin: var(--s-8) 0 var(--s-4) 0;
    text-align: start;
}
.skills__subsection-title {
    font-size: var(--f-lg);
    font-weight: 600;
    margin-block-start: var(--s-12);
    padding-block-end: var(--s-1);
    border-block-end: 2px solid var(--border-primary);
    grid-column: 1 / -1;
    inline-size: 100%;
}
.skills-section-title,
.skills-subsection-title {
    grid-column: 1 / -1;
    width: 100%;
    font-size: var(--f-2xl);
    font-weight: 700;
    color: var(--text-dark);
    margin: 2rem 0 1rem 0;
    text-align: left;
}
.skills-subsection-title {
    font-size: var(--f-xl);
    font-weight: 600;
    margin-top: 3rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--border-primary);
}
#popover {
    position: absolute;
    background-color: var(--popover-bg-color);
    color: var(--popover-text-color);
    padding: 20px;
    border-radius: 16px;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
    max-width: 320px;
    z-index: 1000;
    border: none;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    animation: fadeIn 0.3s ease-out;
    line-height: 1.6;
    font-size: 0.95rem;
    border-left: 4px solid var(--accent-primary);
}
/* ========================================
   EXPERIENCE SECTION (Nested & Logical)
   Modern component architecture with CSS Nesting
   ======================================== */
.experience {
    position: relative;
    overflow: hidden;
    &::before {
        content: '';
        position: absolute;
        inset-block-start: 50%;
        inset-inline-end: 0;
        inline-size: 400px;
        block-size: 400px;
        background: var(--gradient-success);
        opacity: 0.05;
        border-radius: 50%;
        translate: 200px -50%;
        z-index: -1;
    }
}
.experience__list {
    list-style: none;
    padding: 0;
    margin: 0;
    position: relative;
    max-inline-size: 1000px;
    margin-inline: auto;
    &::before {
        content: '';
        position: absolute;
        inset-block: 0;
        inset-inline-start: 50%;
        block-size: 100%;
        inline-size: 1px;
        background: var(--border-primary);
        translate: -50% 0;
        z-index: 0;
    }
}
.experience__item {
    position: relative;
    inline-size: 50%;
    padding-inline: var(--s-10);
    margin-block-end: var(--s-12);
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
    animation-delay: calc(var(--i, 0) * var(--stagger-delay));
    &:nth-child(odd) {
        inset-inline-start: 0;
    }
    &:nth-child(even) {
        inset-inline-start: 50%;
        text-align: start;
    }
    &::before {
        content: '';
        position: absolute;
        inset-block-start: var(--s-6);
        inline-size: 12px;
        block-size: 12px;
        background: var(--bg-primary);
        border: 2px solid var(--accent-primary);
        border-radius: 50%;
        z-index: 1;
        transition: all var(--transition-normal);
        box-shadow: 0 0 0 var(--s-4) var(--bg-primary);
    }
    &:nth-child(odd)::before {
        inset-inline-end: -6px;
    }
    &:nth-child(even)::before {
        inset-inline-start: -6px;
    }
    &:hover::before {
        background: var(--accent-primary);
        transform: scale(1.2);
        box-shadow: 0 0 0 var(--s-6) var(--bg-primary);
    }
    &-date {
        display: inline-block;
        padding: var(--s-1) var(--s-3);
        background: var(--bg-primary);
        color: var(--text-secondary);
        border: 1px solid var(--border-primary);
        border-radius: var(--s-10);
        font-size: var(--f-sm);
        font-weight: 500;
        margin-block-end: var(--s-4);
    }
    &:nth-child(odd) &-date {
        float: inline-end;
    }
    &-company {
        font-weight: 600;
        color: var(--text-primary);
        margin-block-end: var(--s-2);
    }
    &-title {
        font-size: var(--f-lg);
        font-weight: 700;
        color: var(--text-primary);
        margin-block-end: var(--s-2);
    }
    &-description {
        font-size: var(--f-base);
        color: var(--text-secondary);
        line-height: 1.6;
        margin-block-end: var(--s-4);
    }
}
/* Legacy support - transition period */
@keyframes fadeInLeft {
    from {
        opacity: 0;
        transform: translateX(-20px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
/* ========================================
   EDUCATION SECTION (Nested & Logical)
   ======================================== */
.education {
    padding-block: var(--section-padding);
    padding-inline: var(--s-6);
    position: relative;
    overflow: hidden;
    &::before {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, rgba(0, 184, 148, 0.03), rgba(116, 185, 255, 0.03));
        z-index: -1;
    }
}
.education__list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--s-8);
    margin: var(--s-10) auto 0;
    max-inline-size: var(--container-max-width);
}
.education-item {
    display: flex;
    gap: var(--s-5);
    background: var(--bg-secondary);
    border: 1px solid var(--border-primary);
    border-radius: var(--border-radius-md);
    padding: var(--s-6);
    position: relative;
    transition: var(--transition-normal);
    height: auto;
    &:hover {
        transform: translateY(calc(var(--s-1) * 1.5));
        box-shadow: var(--shadow-md);
        border-color: var(--accent-primary);
    }
    &-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        inline-size: 50px;
        block-size: 50px;
        background: var(--gradient-primary);
        border-radius: var(--s-3);
        color: white;
        font-size: var(--f-lg);
        flex-shrink: 0;
    }
    &-content {
        flex: 1;
    }
    &-degree {
        font-size: var(--f-lg);
        font-weight: 700;
        color: var(--text-dark);
        margin-block-end: var(--s-1);
        line-height: 1.3;
    }
    &-institution {
        font-size: var(--f-sm);
        color: var(--text-primary);
        opacity: 0.8;
    }
}
.education-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    background: var(--gradient-primary);
    border-radius: 12px;
    color: white;
    font-size: 1.2rem;
    flex-shrink: 0;
}
.education-content {
    flex: 1;
}
.education-degree {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--text-dark);
    margin-bottom: 5px;
    line-height: 1.3;
}
.education-institution {
    font-size: 0.9rem;
    color: var(--text-primary);
    opacity: 0.8;
}
/* ========================================
   CONTACT SECTION (Nested & Logical)
   ======================================== */
.contact {
    padding-block-end: var(--section-padding);
    position: relative;
    overflow: hidden;
    &::before {
        content: '';
        position: absolute;
        inset-block-end: 0;
        inset-inline-end: 0;
        inline-size: 300px;
        block-size: 300px;
        background: var(--gradient-secondary);
        opacity: 0.05;
        border-radius: 50%;
        translate: 150px 150px;
        z-index: -1;
    }
}
.contact__buttons {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--s-6);
    max-inline-size: var(--container-max-width);
    margin: 0 auto;
}
.contact__buttons {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--s-6);
    max-width: var(--container-max-width);
    margin: 0 auto;
}
/* ========================================
   PROJECTS SECTION (Nested & Logical)
   ======================================== */
.projects {
    position: relative;
    overflow: hidden;
    &::before {
        content: '';
        position: absolute;
        inset-block-start: 0;
        inset-inline-start: 0;
        inline-size: 250px;
        block-size: 250px;
        background: var(--gradient-success);
        opacity: 0.07;
        border-radius: 50%;
        translate: -120px -120px;
        z-index: -1;
    }
    &::after {
        content: '';
        position: absolute;
        inset-block-end: 0;
        inset-inline-end: 0;
        inline-size: 350px;
        block-size: 350px;
        background: var(--gradient-primary);
        opacity: 0.06;
        border-radius: 50%;
        translate: 170px 170px;
        z-index: -1;
    }
}
.projects__container {
    display: grid;
    grid-template-columns: repeat(var(--grid-columns), 1fr);
    gap: var(--grid-gap);
    margin: var(--s-8) auto 0;
    max-inline-size: var(--container-max-width);
    .project-card {
        &:hover .project-image {
            transform: scale(1.03);
        }
    }
}

/* ========================================
   CARD COMPONENT SYSTEM (Unified)
   ======================================== */
/* Base card styles - shared across all card types */
.project-card,
.achievement-card {
    background: var(--bg-secondary);
    border: 1px solid var(--border-primary);
    border-radius: var(--border-radius-md);
    padding: var(--s-6);
    position: relative;
    transition: var(--transition-normal);
    display: flex;
    flex-direction: column;
    height: 100%;
    overflow: hidden;

    &:hover {
        transform: translateY(calc(var(--s-1) * 1.5));
        box-shadow: var(--shadow-md);
        border-color: var(--accent-primary);
    }
}

/* Project card specifics */
.project-card {
    .project-card__image {
        inline-size: 100%;
        block-size: 200px;
        object-fit: cover;
        border-radius: var(--s-3);
        margin-block-end: var(--s-5);
        transition: transform var(--transition-normal);
    }

    .project-card__title {
        font-size: var(--f-xl);
        font-weight: 700;
        color: var(--text-dark);
        margin-block-end: var(--s-3);
        line-height: 1.3;
    }

    .project-card__description {
        font-size: var(--f-md);
        color: var(--text-primary);
        margin-block-end: var(--s-5);
        line-height: 1.6;
        flex-grow: 1;
    }

    .project-card__tags {
        display: flex;
        flex-wrap: wrap;
        gap: var(--s-3);
        margin-block-end: var(--s-5);
    }

    .project-link {
        margin-block-start: auto;
    }

    &:hover .project-card__image {
        transform: scale(1.03);
    }
}

/* Achievement card specifics */
.achievement-card {
    .achievement-icon {
        font-size: var(--f-4xl-plus);
        margin-block-end: var(--s-6);
        inline-size: 72px;
        block-size: 72px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, hsla(var(--brand-hue), var(--brand-sat), 60%, 0.1), rgba(139, 92, 246, 0.1));
        border-radius: var(--s-5);
        color: var(--accent-primary);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
    }

    &:hover .achievement-icon {
        transform: scale(1.1) rotate(5deg);
        background: linear-gradient(135deg, hsla(var(--brand-hue), var(--brand-sat), 60%, 0.2), rgba(139, 92, 246, 0.2));
        box-shadow: 0 var(--s-2) var(--s-4) hsla(var(--brand-hue), var(--brand-sat), 60%, 0.2);
    }
}

.project-tag {
    background: linear-gradient(135deg, hsla(var(--brand-hue), var(--brand-sat), 60%, 0.15), rgba(99, 102, 241, 0.15));
    color: var(--accent-active);
    padding: var(--s-2) calc(var(--s-2) * 1.75);
    border-radius: var(--s-5);
    font-size: var(--f-sm);
    font-weight: 600;
    border: 1px solid hsla(var(--brand-hue), var(--brand-sat), 60%, 0.2);
    [dark-mode] & {
        background: linear-gradient(135deg, hsla(var(--brand-hue), var(--brand-sat), 60%, 0.2), rgba(99, 102, 241, 0.2));
        color: var(--accent-hover);
        border: 1px solid rgba(var(--accent-color-rgb), 0.3);
    }
}
/* Project tags container */
.project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--s-3);
    margin-block-end: var(--s-5);
}
/* Buttons Base Styles */
.skill-tag,
.project-link,
.contact-btn {
    background: var(--gradient-primary);
    color: white;
    border: none;
    cursor: pointer;
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--s-3);
    font-weight: 600;
    font-size: var(--f-md);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 8px 24px var(--accent-glow);
    overflow: hidden;
    border-radius: 28px;
    padding: var(--s-4) calc(var(--s-4) * 1.75);
    z-index: 1;
    text-decoration: none;
    min-height: 48px;
    -webkit-tap-highlight-color: transparent;

    &:hover {
        box-shadow: 0 12px 32px hsla(var(--brand-hue), var(--brand-sat), 60%, 0.4);
        background: var(--gradient-secondary);
        transform: translateY(calc(var(--s-1) * -1)) scale(1.02);
        color: white;
        & i {
            transform: rotate(15deg) scale(1.1);
        }
    }

    &:active {
        box-shadow: 0 6px 16px var(--accent-glow);
        transform: translateY(calc(var(--s-1) * -0.5)) scale(0.98);
    }

    [dark-mode] &:hover {
        background: linear-gradient(135deg, var(--accent-active), var(--accent-active));
        box-shadow: 0 12px 32px rgba(37, 99, 235, 0.5);
    }

    & i {
        transition: transform 0.3s ease;
    }

    &.contact-btn {
        animation: fadeInUp calc(0.5s + var(--i, 0) * 0.1s) ease-out;
    }
}
/* ========================================
   RESPONSIVE TYPOGRAPHY
   Uses clamp() for fluid scaling
   ======================================== */
/* Typography Base Styles */
h1, h2, h3, h4, h5, h6 {
    font-weight: 700;
    color: var(--text-dark);
    line-height: 1.2;
    margin-bottom: 1rem;
}
/* Responsive heading sizes using clamp() */
h1 {
    font-size: clamp(2.5rem, 8vw, 4.5rem);  /* 40px to 72px */
    letter-spacing: -0.03em;
}
h2 {
    font-size: clamp(1.8rem, 4vw, 2.8rem);  /* 28.8px to 44.8px */
    letter-spacing: -0.02em;
}
h3 {
    font-size: clamp(1.3rem, 2.5vw, 1.8rem); /* 20.8px to 28.8px */
}
h4 {
    font-size: clamp(1.1rem, 2vw, 1.4rem);   /* 17.6px to 22.4px */
    font-weight: 600;
}
h5 {
    font-size: clamp(1rem, 1.8vw, 1.25rem);  /* 16px to 20px */
}
h6 {
    font-size: clamp(0.875rem, 1.5vw, 1rem); /* 14px to 16px */
}
p {
    font-size: clamp(0.95rem, 1.5vw, 1.1rem); /* 15.2px to 17.6px */
    color: var(--text-primary);
    line-height: 1.6;
    margin-bottom: 1rem;
}
/* Intro section specific */
#intro h1 {
    margin-bottom: 20px;
    position: relative;
    z-index: 1;
}
#intro h2 {
    font-size: clamp(1.5rem, 3vw, 2.2rem); /* 24px to 35.2px */
    color: var(--accent-primary);
    margin-bottom: 30px;
    font-weight: 500;
    position: relative;
    z-index: 1;
}
#intro p {
    font-size: clamp(1rem, 2vw, 1.25rem);    /* 16px to 20px */
    max-width: 700px;
    margin: 0 auto 40px;
    color: var(--text-secondary);
    position: relative;
    z-index: 1;
}
/* Section titles */
section h2 {
    font-weight: 800;
    margin-bottom: clamp(2rem, 5vw, 3rem);   /* Responsive spacing */
    margin-left: auto;
    margin-right: auto;
    position: relative;
    padding-bottom: 20px;
    max-width: var(--container-max-width);
}
section h2::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 80px;
    height: 4px;
    background: var(--gradient-primary);
    border-radius: 2px;
    transition: width 0.3s ease;
}
section:hover h2::after {
    width: 120px;
}
/* Navigation Base Styles */
header,
.header-controls {
    position: fixed;
    top: 16px;
    background: rgba(255, 255, 255, 0.75);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
    border-radius: 100px;
    border: 1px solid rgba(255, 255, 255, 0.6);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12),
                0 2px 8px rgba(0, 0, 0, 0.06);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 1000;
}
header {
    left: 0;
    right: 0;
    margin: 0 auto;
    width: fit-content;
    max-width: calc(100% - 40px);
    padding: 4px 12px;
}
header:hover {
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15),
                0 4px 12px rgba(0, 0, 0, 0.08);
    background: rgba(255, 255, 255, 0.85);
}
.header-controls {
    right: 20px;
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 6px 12px;
}
[dark-mode] header,
[dark-mode] .header-controls {
    background: rgba(22, 27, 34, 0.75);
    border: 1px solid rgba(255, 255, 255, 0.15);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.5),
                0 2px 8px rgba(0, 0, 0, 0.3);
}
[dark-mode] header:hover,
[dark-mode] .header-controls:hover {
    background: rgba(22, 27, 34, 0.85);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.6),
                0 4px 12px rgba(0, 0, 0, 0.4);
}
/* Navigation links */
nav ul {
    list-style: none;
    display: flex;
    gap: 4px;
    margin: 0;
    padding: 0;
}
nav ul li a,
.lang-btn {
    text-decoration: none;
    color: var(--text-primary);
    font-weight: 500;
    padding: 3px 8px;
    border-radius: 100px;
    transition: all 0.2s ease;
    display: block;
    border: none;
    background: transparent;
    cursor: pointer;
}
nav ul li a {
    font-size: 0.8rem;
}
.lang-btn {
    font-size: var(--f-lg);
    line-height: 1;
}
nav ul li a:hover,
nav ul li a.active {
    color: var(--text-dark);
    background: var(--bg-primary);
}
.lang-btn:hover,
.lang-btn.active {
    background: hsla(var(--brand-hue), var(--brand-sat), 60%, 0.1);
    color: var(--accent-primary);
}
[dark-mode] nav ul li a:hover,
[dark-mode] nav ul li a.active {
    background: rgba(255, 255, 255, 0.1);
}
/* Language switcher */
.lang-switcher {
    display: flex;
    gap: 4px;
}
.contact-buttons {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: var(--s-6);
    max-width: var(--container-max-width);
    margin: 0 auto;
}
/* ========================================
   MOBILE STYLES (max-width: 768px)
   ======================================== */
@media (max-width: 768px) {
    /* Hover states - disable transforms on mobile */
    .skill-tag:hover,
    .contact-btn:hover,
    .project-card:hover,
    .achievement-card:hover,
    .project-link:hover,
    .nav-link:hover {
        transform: none !important;
    }
    .skill-tag:hover .skill-icon,
    .project-card:hover .project-card__image {
        transform: none !important;
    }
    /* Header & Navigation */
    header {
        position: static;
        transform: none !important;
        background: transparent !important;
        backdrop-filter: none !important;
        box-shadow: none !important;
        border: none !important;
        padding: 60px 0 40px;
    }
    [dark-mode] header {
        background: transparent !important;
        box-shadow: none !important;
        border: none !important;
    }
    .hamburger {
        display: flex;
        position: fixed !important;
        top: 10px !important;
        left: 10px !important;
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(12px);
        border-radius: 8px;
        padding: 8px;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.5);
        z-index: 1001 !important;
    }
    [dark-mode] .hamburger {
        background: rgba(22, 27, 34, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.1);
    }
    nav {
        position: fixed;
        top: 0;
        left: -100%;
        width: 280px;
        height: 100vh;
        background: var(--bg-secondary);
        box-shadow: 2px 0 20px rgba(0, 0, 0, 0.1);
        transition: left 0.3s ease;
        z-index: 999;
        padding-top: 80px;
        visibility: hidden;
    }
    nav.active {
        left: 0;
        visibility: visible;
    }
    nav ul {
        display: flex;
        flex-direction: column;
        gap: 0;
        padding: 20px;
    }
    nav ul li {
        width: 100%;
    }
    nav ul li a {
        display: block;
        padding: 12px 16px;
        font-size: 1rem;
        border-radius: 8px;
        width: 100%;
    }
    nav ul li a:hover {
        background: var(--bg-secondary);
    }
    .header-controls {
        position: fixed;
        top: 10px;
        right: 10px;
        padding: 8px 14px;
        gap: 10px;
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(12px);
        border-radius: 8px;
        box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.5);
    }
    [dark-mode] .header-controls {
        background: rgba(22, 27, 34, 0.8);
        border: 1px solid rgba(255, 255, 255, 0.1);
    }
    /* Intro Section */
    #intro {
        padding: 60px 25px;
        text-align: center;
    }
    h1 {
        font-size: 2.2rem;
        margin-bottom: 15px;
        letter-spacing: 1px;
    }
    .header-subtitle {
        font-size: 1.1rem;
        margin-bottom: 25px;
    }
    #intro h2 {
        font-size: 1.4rem;
        text-align: center;
        max-width: 100%;
        word-break: break-word;
        line-height: 1.4;
        padding: 0 30px;
        margin: 0 auto 20px auto;
        white-space: normal !important;
        overflow: visible !important;
        width: auto !important;
        border-right: none !important;
        animation: none !important;
    }
    .typing-effect {
        white-space: normal !important;
        overflow: visible !important;
        animation: none !important;
        border-right: none !important;
        display: block !important;
        max-width: 100% !important;
        text-align: center !important;
        width: auto !important;
    }
    #intro h2,
    #intro .typing-effect {
        hyphens: auto;
        word-wrap: break-word;
        overflow-wrap: break-word;
    }
    .profile-image {
        width: 150px;
        height: 150px;
    }
    .profile-badge {
        width: 50px;
        height: 50px;
        font-size: var(--f-xl);
    }
    /* Sections */
    section {
        padding: calc(var(--section-padding) * 0.5) var(--s-5);
    }
    h2 {
        font-size: 1.8rem;
    }
    /* Skills Section */
    #skills-container {
        grid-template-columns: 1fr;
    }
    .collapsible-content {
        padding: 16px;
        background-color: var(--bg-secondary);
        border-radius: 8px;
        margin-top: 8px;
        transition: all 0.3s ease-out;
        opacity: 0;
        max-height: 0;
        overflow: hidden;
    }
    .skill-tag.active+.collapsible-content {
        opacity: 1;
        max-height: 500px;
        margin-bottom: 16px;
    }
    /* Projects Section */
    #projects-container {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    .project-card {
        padding: 20px;
    }
    .project-title {
        font-size: 1.4rem;
    }
    .project-description {
        font-size: 0.95rem;
    }
    /* Experience Section */
    .experience__list::before {
        left: 20px;
    }
    .experience__item {
        inline-size: 100%;
        inset-inline-start: 0 !important;
        padding-inline-start: 50px;
        padding-inline-end: 0;
        text-align: left !important;
        padding: 20px 0 20px 50px;
    }
    .experience__item::before {
        left: 10px !important;
        right: auto !important;
    }
    .experience__item:nth-child(odd) .experience__item-date {
        float: none;
    }
    /* Achievements Section */
    .achievements-grid {
        grid-template-columns: 1fr;
        gap: 20px;
        padding: 0 15px;
    }
    .achievement-card:nth-child(1),
    .achievement-card:nth-child(2),
    .achievement-card:nth-child(3),
    .achievement-card:nth-child(4) {
        grid-column: span 1;
    }
    .achievement-card {
        padding: 30px 20px;
    }
    .achievement-icon {
        font-size: 2.5rem;
    }
    /* Contact Section */
    .contact-buttons {
        grid-template-columns: 1fr;
    }
    /* Footer */
    footer {
        padding: var(--s-8) 0;
        margin-top: calc(var(--section-padding) * 0.6);
    }
    .footer-content {
        flex-direction: column;
        text-align: center;
        gap: 20px;
    }
    .social-icons {
        justify-content: center;
    }
}
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
.reveal {
    opacity: 1;
    transform: none;
}
.floating {
    animation: floating 3s ease-in-out infinite;
}
@keyframes floating {
    0% {
        transform: translateY(0px);
    }
    50% {
        transform: translateY(-10px);
    }
    100% {
        transform: translateY(0px);
    }
}
h1 {
    text-shadow:
        0 1px 0 #ccc,
        0 2px 0 var(--text-secondary),
        0 3px 0 #bbb,
        0 4px 0 var(--text-tertiary),
        0 5px 0 #aaa,
        0 6px 1px rgba(0, 0, 0, .1),
        0 0 5px rgba(0, 0, 0, .1),
        0 1px 3px rgba(0, 0, 0, .3),
        0 3px 5px rgba(0, 0, 0, .2),
        0 5px 10px rgba(0, 0, 0, .25),
        0 10px 10px rgba(0, 0, 0, .2);
}
.skip-nav {
    position: fixed;
    top: -100px;
    left: 6px;
    background: var(--accent-primary);
    color: white;
    padding: 8px;
    text-decoration: none;
    border-radius: 4px;
    z-index: 10000;
    font-weight: 600;
    transition: top 0.3s;
    opacity: 0;
}
.skip-nav:focus {
    top: 6px;
    opacity: 1;
}
.nav-link:focus-visible,
.lang-btn:focus-visible,
.theme-toggle:focus-visible,
.contact-btn:focus-visible,
.project-link:focus-visible,
.hamburger:focus-visible {
    outline: 3px solid var(--accent-primary);
    outline-offset: 3px;
    box-shadow: 0 0 0 4px var(--accent-primary-glow);
}
.skill-tag:focus-visible {
    outline: 2px solid var(--accent-primary);
    outline-offset: 3px;
    transform: translateY(-2px);
    box-shadow: 0 0 0 3px var(--accent-primary-glow);
}
.nav-link:focus:not(:focus-visible),
.lang-btn:focus:not(:focus-visible),
.theme-toggle:focus:not(:focus-visible),
.contact-btn:focus:not(:focus-visible),
.project-link:focus:not(:focus-visible) {
    outline: none;
    box-shadow: none;
}
@media (prefers-contrast: high) {
    :root {
        --text-color: #000;
        --h2-color: #000;
        --button-text-color: #000;
        --border-color: #000;
    }
    [dark-mode] {
        --text-color: #fff;
        --h2-color: #fff;
        --button-text-color: #fff;
        --border-color: #fff;
    }
}
@media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
    .typing-effect,
    .floating,
    .particle,
    .skeleton-title,
    .skeleton-text,
    .skeleton-tags,
    .skeleton-link {
        animation: none !important;
        border-right: none;
    }
}
@media (min-width: 1200px) {
    h1 {
        font-size: 3.5rem;
    }
    .header-subtitle {
        font-size: 1.6rem;
    }
    .nav-link {
        padding: var(--s-4) calc(var(--s-4) * 1.75);
        font-size: 1.1rem;
        min-width: 120px;
    }
    nav {
        gap: var(--s-4);
    }
    #intro h2 {
        font-size: 2.5rem;
    }
    section {
        padding: calc(var(--section-padding) * 0.8) 0;
    }
    #intro {
        padding-top: var(--section-padding);
        padding-bottom: var(--section-padding);
    }
}
@media (max-width: 480px) {
    #intro h2 {
        font-size: 1.3rem;
        padding: 0 20px;
        line-height: 1.3;
        margin: 0 auto 15px auto;
        letter-spacing: 0.3px;
    }
    .header-subtitle {
        font-size: 1rem;
        padding: 0 20px;
        margin-bottom: 20px;
    }
    h1 {
        font-size: 1.8rem;
        padding: 0 20px;
        letter-spacing: 0.5px;
        margin-bottom: 12px;
    }
    #intro {
        padding: 40px 20px;
    }
    header {
        padding: 50px 0 35px;
    }
    .nav-link {
        padding: 10px 14px;
        font-size: 0.9rem;
    }
    nav {
        padding-left: 15px;
        padding-right: 15px;
    }
}
.stagger-animation {
    opacity: 0;
    transform: translateY(30px);
    animation: staggerFadeIn 0.6s ease forwards;
    /* Stagger delay via inline variable: style="--i: 3" */
    animation-delay: calc(var(--i, 1) * var(--stagger-delay));
}
@keyframes staggerFadeIn {
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
.loading-spinner {
    width: 40px;
    height: 40px;
    border: 4px solid rgba(9, 132, 227, 0.2);
    border-left: 4px solid var(--accent-primary);
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin: 20px auto;
}
@keyframes spin {
    0% {
        transform: rotate(0deg);
    }
    100% {
        transform: rotate(360deg);
    }
}
#achievements {
    padding: var(--section-padding) 0;
    background: var(--bg-primary);
    position: relative;
}
.achievements-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-template-rows: repeat(2, auto);
    gap: var(--s-6);
    max-width: var(--container-max-width);
    margin: 0 auto;
}
.achievement-card:nth-child(1) {
    grid-column: span 2;
    grid-row: span 2;
    background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);
}
.achievement-card:nth-child(2) {
    grid-column: span 2;
}
.achievement-card:nth-child(3) {
    grid-column: span 1;
}
.achievement-card:nth-child(4) {
    grid-column: span 1;
}
@media (max-width: 1024px) {
    .achievements-grid {
        grid-template-columns: repeat(2, 1fr);
    }
    .achievement-card:nth-child(1) {
        grid-column: span 2;
    }
    .achievement-card:nth-child(2) {
        grid-column: span 2;
    }
    .achievement-card:nth-child(3),
    .achievement-card:nth-child(4) {
        grid-column: span 1;
    }
}
@supports (-webkit-backdrop-filter: none) {
    .header-controls {
        background: rgba(255, 255, 255, 0.15);
    }
}
@supports not (display: grid) {
    .achievements-grid {
        display: -webkit-box !important;
        display: -ms-flexbox !important;
        display: flex !important;
    }
    .achievements-grid .achievement-card {
        margin: 10px !important;
    }
}
footer {
    background: linear-gradient(135deg, var(--color-footer-bg) 0%, var(--color-footer-bg-alt) 100%);
    color: var(--text-inverted);
    padding: calc(var(--section-padding) * 0.6) 0 var(--s-8);
    margin-top: var(--section-padding);
    position: relative;
    overflow: hidden;
    [dark-mode] & {
        background: linear-gradient(135deg, var(--bg-tertiary) 0%, var(--bg-primary) 100%);
    }
    &::before {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at 80% 20%, hsla(var(--brand-hue), var(--brand-sat), 60%, 0.15), transparent 50%);
        pointer-events: none;
    }
    &::after {
        content: '';
        position: absolute;
        top: -50%;
        right: -10%;
        width: 500px;
        height: 500px;
        background: radial-gradient(circle, hsla(var(--brand-hue), var(--brand-sat), 70%, 0.15), transparent 70%);
        border-radius: 50%;
        pointer-events: none;
    }
    .footer-content {
        max-width: 1100px;
        margin: 0 auto;
        padding: 0 var(--s-6);
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--s-10);
        position: relative;
        z-index: 1;
        align-items: center;
    }
    .footer-main {
        min-width: 200px;
        p {
            margin: 0;
            font-size: var(--f-sm);
            color: var(--text-footer-secondary);
            line-height: 1.8;
            &:first-child {
                font-weight: 700;
                font-size: var(--f-md);
                margin-bottom: var(--s-2);
                color: var(--text-footer-primary);
                text-shadow: 0 0 20px hsla(var(--brand-hue), var(--brand-sat), 70%, 0.3);
            }
        }
    }
    .footer-social {
        display: flex;
        flex-direction: column;
        gap: var(--s-3);
        h4 {
            font-size: var(--f-sm);
            font-weight: 600;
            margin-bottom: var(--s-2);
            color: var(--text-footer-secondary);
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
    }
    .social-icons {
        display: flex;
        gap: var(--s-3);
        a {
            width: var(--s-10);
            height: var(--s-10);
            display: flex;
            align-items: center;
            justify-content: center;
            background: hsla(var(--brand-hue), var(--brand-sat), 60%, 0.2);
            border: 1px solid hsla(var(--brand-hue), var(--brand-sat), 60%, 0.3);
            border-radius: 10px;
            color: var(--text-footer-primary);
            font-size: var(--f-lg);
            transition: all var(--transition-normal);
            backdrop-filter: blur(10px);
            &:hover {
                background: hsla(var(--brand-hue), var(--brand-sat), 70%, 0.4);
                border-color: hsla(var(--brand-hue), var(--brand-sat), 70%, 0.5);
                transform: translateY(calc(var(--s-1) * -0.75)) scale(1.05);
                box-shadow: 0 var(--s-2) var(--s-4) hsla(var(--brand-hue), var(--brand-sat), 60%, 0.3);
            }
        }
    }
    .footer-bottom {
        max-width: 1100px;
        margin: var(--s-10) auto 0;
        padding: var(--s-5) var(--s-6) 0;
        border-top: 1px solid hsla(0, 0%, 100%, 0.15);
        text-align: center;
        position: relative;
        z-index: 1;
        p {
            font-size: 13px;
            color: var(--text-footer-muted);
            margin: 0;
        }
    }
}
#scroll-to-top {
    position: fixed;
    bottom: 30px;
    right: 30px;
    width: 56px;
    height: 56px;
    background: var(--gradient-primary);
    color: white;
    border: none;
    border-radius: 50%;
    cursor: pointer;
    box-shadow: 0 4px 12px var(--accent-glow);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    opacity: 0 !important;
    visibility: hidden;
    transform: translateY(20px) scale(0.8);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 99;
    pointer-events: none;
}
#scroll-to-top.visible {
    opacity: 1 !important;
    visibility: visible;
    transform: translateY(0) scale(1);
    pointer-events: auto;
}
#scroll-to-top.visible:hover {
    background: var(--gradient-secondary);
    transform: translateY(-8px) scale(1.1);
    box-shadow: 0 8px 24px hsla(var(--brand-hue), var(--brand-sat), 60%, 0.5);
}
#scroll-to-top.visible:active {
    transform: translateY(-4px) scale(1.05);
}
#scroll-to-top i {
    transition: transform 0.3s ease;
}
#scroll-to-top.visible:hover i {
    transform: translateY(-3px);
}
.skill-tag::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0));
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
    pointer-events: none;
}
.skill-tag:hover::after {
    opacity: 1;
}
.project-card.skeleton {
    pointer-events: none;
}
.skeleton-title,
.skeleton-text,
.skeleton-tags,
.skeleton-link {
    background: linear-gradient(
        90deg,
        var(--border-primary) 0%,
        var(--border-secondary) 50%,
        var(--border-primary) 100%
    );
    background-size: 200% 100%;
    animation: skeleton-loading 1.5s ease-in-out infinite;
    border-radius: 4px;
}
.skeleton-title {
    height: 24px;
    width: 70%;
    margin-bottom: 16px;
}
.skeleton-text {
    height: 16px;
    width: 100%;
    margin-bottom: 8px;
}
.skeleton-text:last-of-type {
    width: 80%;
}
.skeleton-tags {
    height: 24px;
    width: 60%;
    margin-top: 16px;
    display: flex;
    gap: 8px;
}
.skeleton-tags::before,
.skeleton-tags::after {
    content: '';
    display: inline-block;
    height: 100%;
    width: 40px;
    background: inherit;
    border-radius: 12px;
    animation: skeleton-loading 1.5s ease-in-out infinite;
}
.skeleton-link {
    height: 16px;
    width: 120px;
    margin-top: 16px;
}
@keyframes skeleton-loading {
    0% {
        background-position: 200% 0;
    }
    100% {
        background-position: -200% 0;
    }
}
@keyframes pulse {
    0%, 100% {
        transform: scale(1);
        opacity: 1;
    }
    50% {
        transform: scale(1.05);
        opacity: 0.8;
    }
}
@keyframes shimmer {
    0% {
        background-position: -1000px 0;
    }
    100% {
        background-position: 1000px 0;
    }
}
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image:
        radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.03) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(139, 92, 246, 0.03) 0%, transparent 50%),
        radial-gradient(circle at 40% 20%, rgba(16, 185, 129, 0.02) 0%, transparent 50%);
    pointer-events: none;
    z-index: 0;
}
strong, b {
    font-weight: 700;
    color: var(--accent-primary);
}
p, li {
    line-height: 1.8;
}
a {
    color: var(--accent-primary);
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
}
a:hover {
    color: var(--accent-active);
}
.floating {
    animation: floatBadge 3s ease-in-out infinite;
}
@keyframes floatBadge {
    0%, 100% {
        transform: translateY(0);
    }
    50% {
        transform: translateY(-10px);
    }
}
#intro > *:not(.shape-1):not(.shape-2):not(.profile-container) {
    max-width: 900px;
    margin-left: auto;
    margin-right: auto;
}
#skills > *,
#experience > *,
#education > *,
#projects > *,
#achievements > * {
    max-width: var(--container-max-width);
    margin-left: auto;
    margin-right: auto;
}
#contact > * {
    max-width: var(--container-max-width);
    margin-left: auto;
    margin-right: auto;
}
#intro h2 {
    text-align: center;
}
